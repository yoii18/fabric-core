[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fabric-core-common"
version = "0.1.0"
description = "Shared utilities for Fabric notebooks"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]